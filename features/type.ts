const today: Date = new Date();

today.getMonth();

const person = {
  age: 20
};

class Color {}

const red = new Color();

let apples = 5;

let colors: string[];

const myData: { x: number; y: number } = {
  x: 1,
  y: 2
};

const logNumber: (i: number) => void = (i: number) => {
  return i;
};

const json = '{"x": 10}';

const coordinate: { x: number } = JSON.parse(json);
